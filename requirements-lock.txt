# Cortex Research Suite — pinned dependencies
# Only includes packages required by cortex-research-suite itself.
# Transitive system packages are NOT included to avoid false Dependabot alerts.
#
# To install: pip install -r requirements-lock.txt
# To regenerate after changes to pyproject.toml:
#   pip install -e ".[all]" && pip freeze | grep -iE "^(anthropic|pyyaml|pytest|pytest-cov|mypy|ruff|click)==" > requirements-lock.txt

# Core (optional — needed for eval_judge.py and API calls)
anthropic==0.84.0

# YAML support (optional — needed for rubrics and config)
PyYAML==6.0.2

# CLI (de-slop-cli package)
click==8.3.1

# Dev tools
pytest==8.3.5
pytest-cov==6.1.1
ruff==0.11.2

# Generated: 2026-03-16
# Python: 3.9.6 (compatible with 3.10-3.12)
# Note: Cortex has ZERO required runtime dependencies (see pyproject.toml).
# The packages above are optional extras. The full pip freeze was removed
# because it included unrelated system packages (langchain, pillow, etc.)
# that triggered false Dependabot alerts.
